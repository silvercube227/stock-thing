# Hosted read-API runtime deps (Render). Mirrors the core deps + the `api` extra in
# pyproject.toml, minus ML/ingestion. We install deps only and run uvicorn from the
# repo root (cwd is on sys.path) rather than packaging the project — avoids the
# installer trying to build `stock-thing` as a wheel.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
python-dotenv>=1.0
asyncpg>=0.29
pyjwt[crypto]>=2.9
yfinance>=0.2.40
